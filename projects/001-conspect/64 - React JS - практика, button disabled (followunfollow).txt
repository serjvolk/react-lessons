В этом уроке реализованно блокировка кнопок follow/unfollow. Чтобы небыло лишних запросов на сервер.
Для этого мы перед отправкой ajax запроса делаем:  props.toggleFollowingProgress(true, u.id);
а после того как нам пришел ajax делаем: props.toggleFollowingProgress(false, u.id);

Сама кнопка теперь выглядит так:
<button disabled={props.followingInProgress.some(id => id === u.id)} onClick={() => {

Логика такая: Когда мы кликаем по кнопке, мы отправляем в state в массив followingInProgress
id пользователя чей ответ от сервера мы еще не получили, далее сама кнопка методом same проверяет, если
в массиве есть id отображаемого пользователя, значит мы блочим кнопку. Когда ajax завершился, мы удаляем из
массива id пользователя.