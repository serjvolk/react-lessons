Кароче говоря в dispatch было много логики if else, поэтому создаются редьюсеры. 
Мы как бы раздробили dispatch на несколько файликов которые в него импортируются.
В данном случае это файлики:
	dialogs-reducer.js
	profile-reducer.js
и эти файлики потом импортируются в state.js.

Дополнительно мы в эти файлики вынесли и ихние ActionCreator-ы а в файлах где они
используются изменили пути к ним.
