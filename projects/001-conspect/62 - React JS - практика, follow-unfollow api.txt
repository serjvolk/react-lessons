В этом уроке мы реализовали подписку и отписку:


запрос выглядит так:

				    axios.post(`https://social-network.samuraijs.com/api/1.0/follow/${u.id}`,{}, {
                                        withCredentials: true,
                                        headers: {
                                            "API-KEY": "9bc59add-c579-472c-a6ae-ed1a5901fb82"
                                        }
                                    }).then(response => {
                                        if(response.data.resultCode == 0){
                                            props.follow(u.id);
                                        }
                                    });

здесь можно заметить что параметры запроса идут третими, это потому что так устроены post и delete запросы,
кароче говоря все запроы кроме get требуют и второй параметр, а само тело запроса иде в третьем параметре.
У нас сейчас второй параметр просто пусто, что это пока не ясно но оно должно быть.