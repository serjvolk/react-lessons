Кароче говоря помимо BLL и ГШ есть еще и DAL уровень. Он отвечает за API запросы(как я понял).

В этом уроке мы вынесли подключения axios в файл api js, а в саму компоненту передаем только 
response.data и больше ничего.

Теперь в компонентах, запросы выглядят так: usersAPI.getUsers(this.props.currentPage, this.props.pageSize).then(data => {

...это удобно.

В api.js есть instance. Это как бы мы формируем axios запрос, собираем его настройки, чтобы каждый раз
когда делаем запрос не повторяться заново.

